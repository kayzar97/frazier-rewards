import WagerRewardsClient from "./WagerRewardsClient";

export const metadata = {
  title: "Wager Rewards | FrazierRewards",
};

export default function WagerRewardsPage() {
  return <WagerRewardsClient />;
}